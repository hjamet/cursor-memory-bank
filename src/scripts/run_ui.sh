#!/bin/bash
# This script is a convenient way to launch the Streamlit UI.
# It ensures that the application is run correctly using the 'streamlit run' command.

echo "Launching Streamlit UI..."
echo "You can view the app in your browser."
echo ""
python -m streamlit run src/ui/app.py 