---
alwaysApply: false
description: Guide méthodologique pour identifier systématiquement l'origine précise d'un bug via hypothèses, logs de débogage et analyse
---

# Règle Enquêteur : Identification Systématique des Bugs 🕵️

## Principe Fondamental : Fail-Fast Appliqué au Débogage

Cette règle implémente le principe "Fail-Fast" dans le contexte du débogage : **identifier rapidement et précisément l'origine du bug sans le masquer**. L'objectif n'est PAS de résoudre le bug, mais de le localiser avec une précision chirurgicale pour permettre une correction ciblée.

**Philosophie** : Un bug mal localisé est un bug qui reviendra. Mieux vaut échouer rapidement avec une origine claire qu'un contournement qui masque la cause racine.

---

## ⛔🚫❌ INTERDICTION ABSOLUE DE CORRECTION ❌🚫⛔

**RÈGLE CRITIQUE** : L'enquêteur doit UNIQUEMENT identifier l'origine du bug. **TOUTE tentative de correction interrompt immédiatement le cycle d'enquête.**

### Interdictions strictes :
- 🚫 **Ne jamais modifier le code** pour "tester une correction"
- 🚫 **Ne jamais proposer de solutions** dans le rapport final
- 🚫 **Ne jamais implémenter de fix** ou workaround
- ❌ **Arrêt immédiat** si l'agent commence à corriger au lieu d'enquêter

**Rappel constant** : Cette règle **IDENTIFIE**, ne corrige pas. Une fois l'origine claire, c'est à l'utilisateur ou à une autre règle de procéder à la correction.

## Processus Méthodologique en 5 Étapes

### ⚠️ RÈGLE CRITIQUE : Indication Obligatoire de l'Étape

L'agent DOIT **toujours** indiquer son étape actuelle avec des titres markdown explicites :
- `## Étape 1.a : Exploration des fichiers de code`
- `## Étape 1.b : Formulation d'hypothèses`
- `## Étape 2 : Placement de logs de débogage`
- `## Étape 3 : Exécution et récupération des logs`
- `## Étape 4 : Analyse des résultats`
- `## Étape 4.a : Questionnement "POURQUOI" obligatoire`
- `## Étape 4.b : Décision de poursuite ou d'itération`
- `## Étape 5 : Nettoyage et rapport final`

---

## Étape 1.a : Exploration des Fichiers de Code 🔍

**Objectif** : Identifier tous les fichiers impliqués et comprendre le flux d'exécution complet.

**Actions** :
- Cartographier les fichiers (via `codebase_search`, `read_file`, `grep`)
- Analyser le parcours d'exécution 
- Identifier les dépendances (variables, fonctions, services)
- Localiser les points critiques (conditions, validations, transformations)

**Sortie** : Liste des fichiers, flux d'exécution, et points de défaillance potentiels.

---

## Étape 1.b : Formulation d'Hypothèses 🎯

**Objectif** : Formuler 3-5 hypothèses précises et testables sur l'origine du bug.

**Approche** :
- Synthétiser les informations de l'étape 1.a
- Identifier les points de défaillance probables (variables, fonctions, conditions)
- Prioriser par probabilité et impact
- Chaque hypothèse doit pointer vers une ligne/instruction spécifique

**Exemple** :
- Hypothèse 1 : `user_id` est `None` car `extract_user_id_from_token()` ligne 23 échoue
- Hypothèse 2 : Exception non catchée dans `db.get_user()` ligne 45
- Hypothèse 3 : Logique inversée dans `verify_signature()` ligne 12

### ⚠️ RÈGLE CRITIQUE 
**Ne jamais se satisfaire de solutions simples** : creuser jusqu'à la ligne exacte, identifier la CAUSE racine, pas les symptômes.

---

## Étape 2 : Placement de Logs de Débogage 📝

**Objectif** : Insérer des logs "avant/après" pour identifier la ligne exacte du bug.

### ⚠️ RÈGLES CRITIQUES POUR LES LOGS

1. **Format dictionnaire obligatoire** : `{"clé": str(valeur)[:limite]}`
2. **Limitation de caractères** : Maximum 100-500 caractères selon l'importance
3. **Exit après boucles** : Protection obligatoire contre les boucles infinies
4. **Logs "sandwich"** : Log AVANT et APRÈS la ligne suspecte

### Format standard des logs
```python
# Log AVANT - tout doit être normal
print({
    "BEFORE_line_47": "true",
    "a": str(a)[:50],
    "b": str(b)[:50],
    "state": "NORMAL"
})

# Ligne suspecte 47
result = a - b  # BUG ICI : devrait être a + b

# Log APRÈS - problème visible ici
print({
    "AFTER_line_47": "true", 
    "result": str(result)[:50],
    "expected": str(a + b)[:50],
    "state": "ANOMALIE_DETECTED" if result != (a + b) else "NORMAL"
})
exit(1)  # Arrêt obligatoire pour analyse
```

---

## Étape 3 : Exécution et Récupération des Logs 🚀

**Objectif** : Lancer le programme et récupérer tous les logs dans les conditions du bug.

**Méthodes** :
- Exécution directe : `python main.py` ou `npm start`
- Script temporaire : Créer `debug_scenario.py` à la racine avec code minimal
- Conditions exactes : Reproduire l'environnement du bug

**Récupération** : Capturer stdout/stderr, préserver l'ordre chronologique.

---

## Étape 4 : Analyse des Résultats 🔍

**Objectif** : Analyser les logs pour identifier les patterns et symptômes du bug.

**Méthodologie** :
1. Examiner les logs pour valeurs inattendues
2. Corréler avec les hypothèses formulées
3. Identifier les symptômes précis
4. Préparer le questionnement "POURQUOI"

**Résultat attendu** : Liste des symptômes clairs avec hypothèses validées/invalidées.

---

## Étape 4.a : Questionnement "POURQUOI" Obligatoire ❓

**Objectif** : Se poser "POURQUOI" pour chaque symptôme et répondre avec certitude absolue.

### ⚠️ RÈGLE CRITIQUE - VALIDATION OBLIGATOIRE

**Avant l'étape 4.b, l'agent DOIT avoir :**

1. **Question "POURQUOI" posée et répondue** pour chaque symptôme
2. **Preuves par logs** que tout est normal AVANT la ligne identifiée
3. **Preuves par logs** que le problème apparaît APRÈS cette ligne

### Processus de validation
- **Identifier le symptôme** → **Poser "POURQUOI"** → **Répondre avec précision ligne/instruction**
- **Vérifier** : État normal AVANT + Problème visible APRÈS + UNIQUE passage problématique

**Si validation échoue** → Retour étape 2 pour logs supplémentaires

### Questions typiques
- POURQUOI cette variable a cette valeur inattendue ?
- POURQUOI cette fonction retourne ce résultat incorrect ?
- POURQUOI cette condition est toujours vraie/fausse ?

---

## Étape 4.b : Décision de Poursuite ou d'Itération 🎯

**Objectif** : Décider si l'origine du bug est suffisamment claire pour le rapport final.

### Checkpoint de Validation : 3 Questions Obligatoires

**Avant l'étape 5, répondre OUI avec preuves :**

1. **Ai-je la ligne EXACTE du bug ?** (pas "dans la fonction X")
2. **Ai-je des logs AVANT cette ligne** prouvant que tout est normal ?
3. **Ai-je des logs APRÈS cette ligne** prouvant l'anomalie ?

**Décision** :
- 3 OUI → ✅ Passage à l'étape 5
- 1 NON → ❌ Retour étape 2

### ⚠️ RÈGLE DE PROFONDEUR : Fonctions Locales

**Si la ligne identifiée est un APPEL de fonction locale** (pas bibliothèque externe) :
- ❌ Tu ne peux PAS t'arrêter
- ❌ Tu dois RENTRER dans cette fonction
- ❌ Continue jusqu'à trouver l'instruction primitive (assignation, condition, opération, retour)

**Exemples** :
- `result = calculate_total(items)` → RENTRER DEDANS
- `data = json.loads(raw_data)` → Ne pas rentrer (bibliothèque externe)

### Décisions INACCEPTABLES
- Arrêt sur symptôme ("`user_id` est None")
- Localisation vague ("dans la fonction X")
- Justification sans preuves ("ça doit être ligne 15")

---

## Étape 5 : Rapport Final 📋

**Objectif** : Fournir un résumé précis de l'origine du bug identifié.

### Rapport obligatoire
- **Bug identifié** : Description du comportement problématique
- **Origine précise** : Fichier, fonction, ligne exacte
- **Cause racine** : Pourquoi le bug se produit
- **Hypothèses validées/invalidées** : Liste avec logs décisifs
- **Conditions de reproduction** : Environnement exact nécessaire

**Rappel** : Ce rapport identifie l'origine, ne propose AUCUNE correction.

---

## Mini-Cas Pratiques

### Cas 1 : Fonction Locale À Explorer
**Problème** : `result = calculate_total(items)` retourne 0 au lieu de 150.
**Action** : RENTRER dans `calculate_total()` pour trouver l'instruction primitive problématique.

### Cas 2 : Logs Avant/Après
```python
# AVANT - tout normal
print({"BEFORE_line_47": "true", "a": str(a)[:50], "b": str(b)[:50]})
result = a - b  # BUG ICI : devrait être a + b  
# APRÈS - anomalie détectée
print({"AFTER_line_47": "true", "result": str(result)[:50], "expected": str(a + b)[:50]})
```

### Cas 3 : Arrêt Prématuré Évité
**Mauvais** : "Le bug vient de l'authentification car `user_id` est None"
**Bon** : "Ligne 23 dans `extract_user_id()` : `return None` au lieu de `return payload['user_id']`"

---

## Règles de Sécurité et Erreurs à Éviter

### ⚠️ Interdictions Absolues
- **Ne jamais simuler** : Tester le véritable code uniquement
- **Ne jamais masquer** : Logs doivent révéler, pas cacher
- **Ne jamais contourner** : Identifier la cause, pas les symptômes
- **Ne jamais proposer de corrections** : UNIQUEMENT identifier l'origine

### Erreurs Fréquentes (2-3 lignes chacune)
- **Arrêt sur symptôme** : "`user_id` est None" → Creuse jusqu'à la ligne exacte
- **Localisation vague** : "Dans le module auth" → Fichier + fonction + ligne + instruction précise  
- **Justification sans preuves** : "Ça doit être ligne 15" → Logs avant/après obligatoires
- **Manquer validation avant/après** : Preuve que tout est normal avant ET problème après
- **Confondre cause/conséquence** : Remonter à la PREMIÈRE instruction incorrecte
- **Arrêt sur fonction locale** : Si appel de fonction locale → RENTRER dedans jusqu'à instruction primitive

---

## Activation de la Règle

Cette règle est activée manuellement par l'utilisateur lors de sessions de débogage. Pour l'utiliser :

1. **Déclencher** : "Utilise la règle enquêteur pour identifier l'origine de ce bug"
2. **Contextualiser** : Fournir la description du bug et les conditions de reproduction
3. **Suivre** : L'agent suivra automatiquement le processus en 5 étapes
4. **Valider** : Vérifier que le repo est propre après l'enquête

**Rappel** : Cette règle identifie, ne corrige pas. Une fois l'origine claire, c'est à l'utilisateur ou à une autre règle de procéder à la correction.