---
alwaysApply: false
description: Vérification systématique de la racine du repository pour identifier les fichiers inappropriés
---

# Étape 01 : Vérification Racine du Repository 🔍

## ⚠️ SYSTÈME À ÉTATS - RAPPEL OBLIGATOIRE

Vous êtes actuellement dans une **machine à états stricte** pour le nettoyage du repository :

### Règles de fonctionnement
- ✅ **UNIQUEMENT** effectuer les actions décrites dans cette règle
- ❌ **INTERDICTION ABSOLUE** de faire autre chose (même si ça semble utile)
- ❌ **NE RIEN CASSER** : préserver fonctionnalité existante
- ✅ **OBLIGATION** d'invoquer la règle suivante en fin d'étape avec l'outil `fetch_rules`

### Machine à états
Cette étape fait partie d'un processus en 6 étapes :
`00-start → 01-root-check → 02-select-folder → 03-explore-folder → 04-clean-files ⟲ → 05-update-docs`

**Votre étape actuelle** : 01-root-check - Vérification racine du repository

---

## Objectif de l'Étape 01

**Identifier et nettoyer les fichiers qui n'ont pas leur place à la racine du repository** pour maintenir une structure propre et organisée.

### Actions Obligatoires à Effectuer

1. **Lister tous les fichiers à la racine** :
   - Utiliser `list_dir` pour obtenir la liste complète des fichiers/dossiers racine
   - Identifier chaque élément présent

2. **Catégoriser les fichiers selon leur légitimité** :
   - ✅ **Fichiers légitimes** : README.md, package.json, install.sh, .gitignore, etc.
   - ❌ **Fichiers inappropriés** : logs, scripts temporaires, documentation longue, fichiers de cache

3. **Identifier les fichiers à nettoyer** :
   - Fichiers de logs : `*.log`, `*.tmp`, `*.temp`
   - Fichiers de cache : `*.cache`, `__pycache__/`, `.DS_Store`
   - Documentation longue : fichiers `.md` autres que README.md
   - Scripts temporaires : fichiers de test, scripts jetables

4. **Nettoyer les fichiers inappropriés** :
   - Supprimer les fichiers temporaires après vérification
   - Déplacer la documentation longue vers `documentation/`
   - Nettoyer les fichiers de cache

### Types de Fichiers à Nettoyer

#### Fichiers Temporaires et Cache
- **Extensions** : `.tmp`, `.temp`, `.cache`, `.log`, `.bak`, `.swp`, `.pyc`
- **Dossiers** : `__pycache__/`, `.DS_Store`, `.vscode/`, `.idea/`
- **Action** : Suppression immédiate après vérification qu'ils ne sont pas utilisés

#### Documentation Inappropriée
- **Fichiers** : `*.md` autres que `README.md`
- **Action** : Déplacer vers `documentation/` avec nom approprié

#### Scripts Temporaires
- **Patterns** : `test_*.py`, `temp_*.sh`, `debug_*.js`
- **Action** : Supprimer si vraiment temporaires, sinon déplacer vers `scripts/`

### Sortie Attendue

À la fin de cette étape, vous devez fournir :
- **Liste des fichiers** présents à la racine (avant nettoyage)
- **Fichiers identifiés** comme inappropriés
- **Actions effectuées** : suppressions, déplacements, nettoyages
- **État final** de la racine après nettoyage

### Exemple de Sortie

```
Fichiers à la racine identifiés :
- README.md ✅ (légitime)
- package.json ✅ (légitime)
- install.sh ✅ (légitime)
- debug.log ❌ (à supprimer)
- temp_script.py ❌ (à supprimer)
- architecture.md ❌ (à déplacer vers documentation/)

Actions effectuées :
- Supprimé : debug.log, temp_script.py
- Déplacé : architecture.md → documentation/architecture.md

État final : Racine propre avec uniquement les fichiers légitimes
```

### Règles de Sécurité

1. **Principe Fail-Fast** : si un fichier semble important, s'arrêter et demander confirmation
2. **Jamais de suppression aveugle** : toujours vérifier l'utilisation avant suppression
3. **Conserver l'intégrité** : préserver le contenu fonctionnel existant
4. **Documenter les actions** : noter chaque suppression/déplacement

---

## Transitions Possibles

**Toujours vers** : `02-select-folder` - Sélection du dossier à traiter via la table des matières

**Action requise** : Une fois la racine nettoyée et documentée, invoquer `fetch_rules` avec le nom `02-select-folder` pour passer à la sélection du dossier à traiter.